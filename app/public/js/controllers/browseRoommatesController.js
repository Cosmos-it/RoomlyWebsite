/**
 * Created by Taban on 12/30/15.
 */
// create the controller and inject Angular's $scope
roomly.controller('BrowseRoommatesController', function ($scope) {
    // create a message to display in our view
    $scope.about = 'Everyone come and see how good I look!';

    /**
     *  This part reads data from the PHP API to display all the contents
     *  for the users searching for roommates.
     *
     *  The search category could be reduced to
     *  find by users who owns rooms and
     *  or those that don't have rooms.
     *  The program should be able to load the all location close the user
     *  searching for the roommates.
     *
     * */

});
